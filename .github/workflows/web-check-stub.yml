name: "Platform - Web - Format & Type check"
on:
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          if grep -q FAIL web_status.txt; then echo "::error::web failed"; exit 1; fi
          echo "web ok"
